# Prologclp-ejemplo

En este repositorio está el ejemplo básico de prolog(clp) basado en restricciones







En el archivo ejemplo.pl viene un código con al menos 3 tipos de restricciones para hallar el valor de X y Y

